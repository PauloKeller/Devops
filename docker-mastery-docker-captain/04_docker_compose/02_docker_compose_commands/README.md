# Docker compose CLI

- runs up a docker compose
```
docker-compose up
```

- run on background
```
docker-compose up -d
```

- shut down a docker compose
```
docker-compose down
```

- log a docker compose
```
docker-compose logs
```